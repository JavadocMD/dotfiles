- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    ~/.inputrc: inputrc
    ~/.vim: vim
    ~/.vimrc: vimrc

- shell:
  - [git submodule update --init  --recursive, Installing submodules]
